/**
 * Re-export the corridor hero from ui for backwards-compatible imports.
 * Prefer importing from `@/components/ui/image-stream-hero`.
 */
export {
  ImageStreamHero,
  type ImageStreamHeroProps,
  type StreamImage,
  type CorridorPath,
} from '@/components/ui/image-stream-hero';

export { default } from '@/components/ui/image-stream-hero';
